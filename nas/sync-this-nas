#!/bin/bash
# ~/repos/scripts/nas/sync-this-nas
# Reminder script — sync-this-nas runs on WBU, not FleetNAS.

echo ""
echo "  To sync NAS scripts, run on WBU:"
echo "    ./sync-this-nas \"your commit message\""
echo ""
echo "  ⚠️  DELETIONS: tar sync is one-way for deletes."
echo "  If you delete a file on FleetNAS, also delete it on WBU:"
echo "    rm ~/repos/scripts/nas/<filename>"
echo "  Then run sync-this-nas on WBU to commit the deletion."
echo ""
